codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        flags:
          - unit
    patch:
      default:
        target: auto
        threshold: 1%
